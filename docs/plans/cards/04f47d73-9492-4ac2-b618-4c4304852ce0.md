# Plan Card: Verify Version Endpoint — Staging

**Card ID:** 04f47d73-9492-4ac2-b618-4c4304852ce0  
**Host:** https://storefront-01-staging-5d1f3dea031e87e1.olympus-ai.cloud

## Goal

Confirm the deployed staging build responds correctly to:
- `GET /api/health/version` → 200 `{ status: "ok", app: "storefront-01", version: <non-empty> }`
- `GET /api/health` → 200 `{ status: "ok" }`

## Implementation

The `/api/health/version` route is implemented in:
- `backend/src/health/health.controller.ts` — `@Get('version')` handler
- `backend/src/health/app-version.ts` — `APP_NAME = 'storefront-01'`, `resolveAppVersion()` reads from `package.json`

---

## PROGRESS LOG

### 2026-09-09 — Branch push (attempt 1)

**Deployed commit SHA:** `9ff4e2255ea313049b81f0f73e74d40bdcf65c1b`

**Probe results (pre-deploy, staging still on previous build):**

```
$ curl -fsS https://storefront-01-staging-5d1f3dea031e87e1.olympus-ai.cloud/api/health
{"status":"ok"}
EXIT: 0
```

```
$ curl -fsS https://storefront-01-staging-5d1f3dea031e87e1.olympus-ai.cloud/api/health/version
curl: (22) The requested URL returned error: 404
EXIT: 22
```

**Status:** `/api/health/version` returns 404 — staging has not yet deployed commit `9ff4e2255`. The handler is present in the branch. Awaiting staging redeploy.
